<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.common.dao.VehicleInfoDao">
	
	<!-- 查询数据
	<select id="findList" resultType="VehicleInfo">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	<select id="findVehicleName" resultType="VehicleInfo">
		select id,niankuan,chexingmingcheng
		from common_vehicle_info
		where chexi_id=#{chexiId}
	</select>

	<!-- 查询车型 -->
	<select id="getCarModel" resultType="VehicleInfo">
		SELECT id,chexingmingcheng,changshangzhidaojia_yuan,ranyoubiaohao,qigangshu_ge,huanbaobiaozhun,biansuxiangleixing,
		qudongfangshi,absfangbaosi
		FROM common_vehicle_info
		WHERE chexing_id = #{chexingId}
	</select>

</mapper>