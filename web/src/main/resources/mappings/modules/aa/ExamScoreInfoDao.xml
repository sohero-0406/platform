<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.aa.dao.ExamScoreInfoDao">
	
	<!-- 查询数据
	<select id="findList" resultType="ExamScoreInfo">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->

	<select id="getExamScoreInfo" resultType="ExamScoreInfo">
		select
		  aExam.id as id,
		  aExam.name as name,
		  aExam.type as type,
		  bExam.id as sonId,
		  bExam.name as sonName
		 from aa_exam_score_info aExam
		LEFT JOIN aa_exam_score_info  bExam on aExam.type = bExam.super_type
		where aExam.super_type is null

	</select>
	
</mapper>